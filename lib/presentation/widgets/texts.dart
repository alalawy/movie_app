import 'package:flutter/material.dart';
import 'package:flutter/src/widgets/container.dart';
import 'package:flutter/src/widgets/framework.dart';

class TextH10 extends StatelessWidget {
  TextH10({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 12,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}

class TextH9 extends StatelessWidget {
  TextH9({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 14,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}

class TextH8 extends StatelessWidget {
  TextH8({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 16,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}

class TextH7 extends StatelessWidget {
  TextH7({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 18,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}

class TextH6 extends StatelessWidget {
  TextH6({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 20,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}

class TextH5 extends StatelessWidget {
  TextH5({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 22,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}

class TextH4 extends StatelessWidget {
  TextH4({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 24,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}

class TextH3 extends StatelessWidget {
  TextH3({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 26,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}

class TextH2 extends StatelessWidget {
  TextH2({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 28,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}

class TextH1 extends StatelessWidget {
  TextH1({super.key, this.text, this.color, this.weight});
  String? text;
  Color? color;
  FontWeight? weight;

  @override
  Widget build(BuildContext context) {
    return Text(
      '$text',
      style: TextStyle(
          fontSize: 30,
          color: color ?? Colors.white,
          fontFamily: 'Montserrat',
          fontWeight: weight),
    );
  }
}
